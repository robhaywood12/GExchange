<!DOCTYPE html>
<html>
    <head>
        <title>Gamer Exchange</title>
        <!-- Latest compiled and minified CSS -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    </head>
    <body>
        
        <nav class="navbar navbar-default">
          <div class="container-fluid">
            <div class="navbar-header">
              <a class="navbar-brand" href="#">
                <span class="glyphicon glyphicon-link" aria-hidden="true"></span>
                Gamer Exchange!
              </a>
            </div>
            <div class="collapse navbar-collapse">
                <p class="navbar-text navbar-left"><a href="/search" class="navbar-link">Search</a></p>
                <!-- if user isnt logged in -->
                <% if(!currentUser) { %>
                  <p class="navbar-text navbar-right"><a class="navbar-link" href="/login">Login</a></p>
                  <p class="navbar-text navbar-right"><a class="navbar-link" href="/register">Sign Up</a></p>
                <% } else { %>
                <!-- else -->
                  <p class="navbar-text navbar-right">Signed In As <a href="#" class="navbar-link"><%= currentUser.username %></a></p>
                  <p class="navbar-text navbar-right"><a class="navbar-link" href="/logout">Log Out</a></p>
                <% } %>
            </div>
          </div>

        </nav>

        
        
    